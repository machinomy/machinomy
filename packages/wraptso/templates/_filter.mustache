{{this.name}}: {
  ({{#if inputs}}filter: { {{#each inputs}}{{name}}?: {{#inputType type}}{{/inputType}}{{#unless @last}}, {{/unless}}{{/each}} }, {{/if}}options?: Web3.FilterObject): Web3.FilterResult
}
